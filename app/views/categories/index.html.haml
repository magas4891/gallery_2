= render 'layouts/slider'
= render 'layouts/menu'
%section#categories.container
  .big_title
    .big_title_txt.montserrat_font Categories
    .text_und_big_title
      For ease of navigation, all images are divided into categories.
    .big_title_txt_separ.background_color
  .section_content.category_section
    .category_items
      - @categories.each do |category|
        .category_item.web.media.print
          = image_tag category.images.first.picture.small.url
          .category_item_info.text-center
            .category_item_title
              = category.name
            %i.category_item_sep.background_color
            .category_item_p
              = category.description
            .category_item_btnblock
              %a.category_item_btn.background_color.linkz_transition{:href => category_path(category), :role => "button"} #{t('categories.index.show')}


    -#- @categories.each do |category|
    -#  .col-md-4
    -#    %h2= category.name
    -#    %p= category.description
    -#
    -#    %p
    -#      %a.btn.btn-secondary{:href => category_path(category), :role => "button"} #{t('categories.index.show')}
    -#
    -#    - if current_user
    -#      -  if category.user_id == current_user.id
    -#        %p= link_to t('.destroy'), category_path(category), method: :delete, data: { confirm: 'Are you sure?' }
    -#      - else
    -#        %p
    -#
    -#    %td= category.follows.count
    -#    %td= (category.follows.count) == 1 ? t('activerecord.attributes.follow.follower') : t('activerecord.attributes.follow.followers')

-#.container
-#  .row
-#    - @categories.each do |category|
-#      .col-md-4
-#        %h2= category.name
-#        %p= category.description
-#
-#        %p
-#          %a.btn.btn-secondary{:href => category_path(category), :role => "button"} #{t('categories.index.show')}
-#
-#        - if current_user
-#          -  if category.user_id == current_user.id
-#            %p= link_to t('.destroy'), category_path(category), method: :delete, data: { confirm: 'Are you sure?' }
-#          - else
-#            %p
-#
-#        %td= category.follows.count
-#        %td= (category.follows.count) == 1 ? t('activerecord.attributes.follow.follower') : t('activerecord.attributes.follow.followers')

-#.container
-#  / Example row of columns
-#  .row
-#    .col-md-4
-#      %h2 Heading
-#      %p Donec id elit non mi porta gravida at eget metus. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus. Etiam porta sem malesuada magna mollis euismod. Donec sed odio dui.
-#      %p
-#        %a.btn.btn-secondary{:href => "#", :role => "button"} View details »
-#    .col-md-4
-#      %h2 Heading
-#      %p Donec id elit non mi porta gravida at eget metus. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus. Etiam porta sem malesuada magna mollis euismod. Donec sed odio dui.
-#      %p
-#        %a.btn.btn-secondary{:href => "#", :role => "button"} View details »
-#    .col-md-4
-#      %h2 Heading
-#      %p Donec sed odio dui. Cras justo odio, dapibus ac facilisis in, egestas eget quam. Vestibulum id ligula porta felis euismod semper. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus.
-#      %p
-#        %a.btn.btn-secondary{:href => "#", :role => "button"} View details »

%p
  = link_to t('.add_new_category'), new_category_url

= paginate @categories

-#= link_to 'Add new image', new_category_image_path
