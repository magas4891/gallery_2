%p= t('devise.mailer.email_changed.greeting', recipient: @email)
- if @resource.try(:unconfirmed_email?)
  %p= t('devise.mailer.email_changed.subject', email: @resource.unconfirmed_email)
- else
  %p= t('devise.mailer.email_changed.message', email: @resource.email)
