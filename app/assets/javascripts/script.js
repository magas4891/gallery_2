/* JS for Plugins */

$(document).ready(function(){


    /* Sticky menu */
	$(".navsticky").sticky({
		topSpacing:0,
	});


    /* Nice Scroll */
    $("html").niceScroll({
        cursorwidth: '10px',
        scrollspeed: '60',
        mousescrollstep: '45'
    });


});
